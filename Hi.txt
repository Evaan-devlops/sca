Perform only this focused correction. Do not scan or redesign the full repository.

Read only:

- ".github/copilot-instructions.md"
- ".github/agents/sop-operator.agent.md"
- ".github/agents/tool-builder.agent.md"
- ".github/skills/sop-execution/SKILL.md"
- ".github/skills/tool-builder/SKILL.md"
- ".github/mcp.json"
- ".vscode/mcp.json" if present

Tasks:

1. Persist these rules in ".github/copilot-instructions.md":
   
   - "<name>: tool" means all following related steps form one cohesive business capability candidate.
   - It never means one tool per click, fill, wait, or verify step.
   - Tools are built only after successful execution, verified route/postconditions, and a validated build-request JSON.
   - "get_code will be created later" and "add_website will be created later" are deferred branch placeholders. Do not invent or build them now.

2. Update "sop-operator.agent.md" so plain-text notes are automatically converted into a canonical SOP before execution.

3. Update "tool-builder.agent.md" so it refuses to build without an exact validated build-request path.

4. Create ".github/skills/notes-to-sop/SKILL.md" with valid frontmatter and the notes-conversion workflow.

5. Verify Faststack through one actual MCP health/bootstrap tool call.
   
   - Do not perform a broad repository scan.
   - If Faststack is unavailable, report "FASTSTACK_NOT_AVAILABLE".
   - Continue this focused local-file task without pretending Faststack was used.

6. Interpret the supplied notes as:
   
   - "authentication: tool" → one capability: "authenticate_to_onetrust"
   - "sanity: tool" → one capability: "search_website_and_route"
   - "get_code" → deferred capability
   - "add_website" → deferred capability

7. Do not create any tools during this task.

8. Automatically create and write the new SOP source using the canonical fields:
   
   "step, step_id, action, element_name, element_type, value, expect, dependency, condition, condition_timing, if_true, if_false, notes, confirmation"

9. Use input placeholders:
   
   - "{{input.email_address}}"
   - "{{input.website_url}}"
   
   Do not hardcode credentials or task-specific values.

10. Use this capability grouping:

Capability: "authenticate_to_onetrust"

Include:

- Navigate to the OneTrust websites URL.
- Wait until the page is fully prepared.
- Fill the Email Address textbox.
- Click Next.
- Pause for manual authentication if password, OTP, MFA, or SSO approval is required.
- Wait for the browser to return to the OneTrust websites page.
- Verify that the websites page is completely rendered.

Capability: "search_website_and_route"

Include:

- Verify the websites page is ready.
- Locate the Search textbox.
- Enter the supplied website URL.
- Press Enter.
- Wait for the results list.
- Check for an exact URL match.
- Branch to "get_code" when found.
- Branch to "add_website" when absent.

11. Treat "get_code" and "add_website" as deferred branch destinations.

- Preserve their branch references.
- Do not create fake implementation steps.
- Do not build tools for them.
- Mark them as deferred/unresolved capabilities.

12. Write the SOP to "SOP/sources/" using the project’s preferred supported source format.

13. Validate the new SOP automatically.

14. Generate its Markdown and execution manifest automatically.

15. Run the canonical task dry-run and report:

- SOP ID and source path
- goal
- ordered steps
- capabilities
- required inputs
- branch graph
- deferred capabilities
- tool candidates
- per-capability mode
- validation and conversion results

16. Do not ask me for approval, confirmation, or permission at any point.

17. When something is ambiguous:

- make the safest reasonable assumption based on the notes and repository schema;
- record the assumption in the SOP notes and final report;
- do not stop to ask a question;
- never invent passwords, credentials, legal decisions, destructive actions, or missing deferred business procedures.

18. Do not run the full test suite, package the repository, create tools, or modify browser-worker architecture.

Complete the listed changes and report the exact files written and commands executed.
